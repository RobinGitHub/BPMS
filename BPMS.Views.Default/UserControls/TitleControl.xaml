<UserControl x:Class="BPMS.Views.Default.TitleControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="27" d:DesignWidth="100" BorderBrush="#FFCCCCCC">
    <UserControl.Resources>
        <Style TargetType="Grid">
            <Setter Property="Background">
                <Setter.Value>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                        <GradientStop Offset="0" Color="#FEFEFE" />
                        <GradientStop Offset="0.5" Color="#F6F6F6" />
                        <GradientStop Offset="1" Color="#F1F1F1" />
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <TextBlock x:Name="txtTitle" Margin="5, 0" VerticalAlignment="Center" />
    </Grid>
</UserControl>
